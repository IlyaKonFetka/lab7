package org.example.cmd.commands;

import org.example.cmd.utils.Command;

public class MinByWeight_Description extends Command {
    public MinByWeight_Description() {
        super("min_by_weight", "вывести элемент с минимальным weight");
    }
}

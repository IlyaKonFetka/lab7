package org.example.cmd.commands;

import org.example.cmd.utils.Command;

public class RemoveByID_Description extends Command {
    public RemoveByID_Description() {
        super("remove_by_id", "удалить элемент коллекции по id");
    }
}

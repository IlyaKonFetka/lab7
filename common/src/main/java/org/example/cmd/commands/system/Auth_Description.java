package org.example.cmd.commands.system;

import org.example.cmd.utils.Command;

public class Auth_Description extends Command {
    public Auth_Description() {
        super("auth", "авторизировать существующего пользователя");
    }
}
